function printRepeating(arr , size)
    {
        var i, j;
        console.log("Repeated Elements are :");
        for (i = 0; i < size; i++)
        {
            for (j = i + 1; j < size; j++)
            {
                if (arr[i] == arr[j])
                    console.log(arr[i] + " ");
            }
        }
    }
 
var arr = [1, 1, 1, 5, 2, 3, 8];
var arr_size = arr.length;
printRepeating(arr, arr_size);
 
